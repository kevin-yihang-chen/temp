# 项目代理工作规范

本文件适用于整个仓库。目标是形成达到 ECCV/ICCV/CVPR 投稿标准、具有明确
新颖性、可靠实验支持与完整可复现性的研究成果。

## 每轮启动顺序

1. 读取本文件、`PLANS.md`、`PROJECT_STATUS.md`、`EXPERIMENTS.md`。
2. 检查最近提交、工作树、关键结果文件与正在运行/排队的 Slurm 任务。
3. 识别当前主要科学瓶颈，并选择信息价值最高且成本合理的下一步。
4. 不以聊天记录代替仓库证据；动态状态必须重新查询。

## 科研闭环

每条路线必须遵循：问题与差距分析 → 明确假设 → 最小实验 → 结果诊断 →
扩展实验 → 消融与鲁棒性验证 → 结论与下一步。优先建立可靠强基线，再
评价新方法。不得隐藏负结果、泄漏受保护数据、事后修改指标定义或夸大结论。

同一路线连续失败时，先检查假设、数据、评测协议、依赖与实现；只有新实验能
区分不同原因假设时才继续。不得仅更换随机种子或扩大算力重复无信息失败。

## 实验与算力

- 长或昂贵实验前必须做真实输入 smoke test，检查正确性、显存、吞吐、预计
  总耗时和输出空间。
- 根据信息价值、排队时间、运行时间、显存、扩展效率和 GPU-hours 选择资源；
  仅在环境支持且净收益明确时使用多 GPU。
- 长任务必须支持 checkpoint、resume 和失败诊断。
- 所有提交的计算任务使用 `yihangc@connect.hku.hk` 的 `--mail-type=ALL`，覆盖
  开始、结束、失败、中断、超时及其他状态变化。
- Hugging Face 凭证只能通过运行环境变量注入，不得写入仓库、日志、命令输出
  或实验文档。离线缓存可用时优先离线运行并清除凭证变量。
- 当前集群无法提交纯CPU任务。

## 记录与验证

每个实验必须在 `EXPERIMENTS.md` 记录：假设、代码 commit、数据版本/哈希、
配置、随机种子、命令、环境、资源消耗、结果、日志、结论和下一步。阶段结束
后同步更新 `PROJECT_STATUS.md`，包含做了什么、证据、当前最佳结果、失败或
风险、下一步最优行动。

代码完成后运行相关单测、静态检查、shell 语法检查和最小真实验证，并执行
`git diff --check` 与 diff 自查。关键结论必须绑定不可变哈希和可复现命令。

## 数据与版本控制

- 官方 train 可用于明确标注的开发/OOF 研究；validation、test 与 reserve 的
  开放必须由预先冻结的协议授权。
- outcome-free 特征、训练标签、校准数据和正式评测数据必须区分角色并做哈希
  绑定。
- 仓库修改默认只保留在本地。只有用户明确要求或批准时才 push GitHub。
- 保留失败日志和负结果；不得覆盖已有实验输出，恢复任务必须验证原设置与哈希。

## 停止并询问用户的条件

仅在缺少必要权限/数据许可、操作不可逆、预计资源超过配额，或关键科学定义无法
从仓库确定时停止。报告精确阻塞点和所需的最小决策；其他可逆、低风险事项自行
作出合理假设并记录。
